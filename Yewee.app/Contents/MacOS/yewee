#!/bin/bash
# Yewee launcher app: opens Terminal running the real launcher script,
# so output stays visible and camera permission belongs to Terminal.
# The .app lives in the repo root; resolve it relative to this bundle.
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
exec open -a Terminal "$ROOT/Yewee Mac.command"
